//===-- Configuration -------------------------------------------*- C++ -*-===//
///
/// \file
/// \brief 配置。
/// \details 名称、版本、作者、版权等信息。
///
/// \version 2019-06-24
/// \since 2017-01-12
/// \authors zhengrr
/// \copyright Unlicense
///
//===----------------------------------------------------------------------===//

#pragma once
#ifndef RRLIBX_CFG_HXX_
#define RRLIBX_CFG_HXX_

#include <cstdint>

/// \brief 产品名。
#define PRODUCT_NAME          "@PRODUCT_NAME@"

/// \brief 产品版本。
#define PRODUCT_VERSION       "@PRODUCT_VERSION@"

/// \brief 产品主版本号。
#define PRODUCT_VERSION_MAJOR @PRODUCT_VERSION_MAJOR@

/// \brief 产品次版本号。
#define PRODUCT_VERSION_MINOR @PRODUCT_VERSION_MINOR@

/// \brief 产品补丁版本号。
#define PRODUCT_VERSION_PATCH @PRODUCT_VERSION_PATCH@

/// \brief 产品微调版本号。
#define PRODUCT_VERSION_TWEAK @PRODUCT_VERSION_TWEAK@

/// \brief 产品作者。
#define PRODUCT_AUTHORS       "@PRODUCT_AUTHORS@"

/// \brief 产品许可。
#define PRODUCT_LICENSE       "@PRODUCT_LICENSE@"

namespace rrlibx {
constexpr char product_name[] {PRODUCT_NAME};                           ///< 产品名。
constexpr char product_version[] {PRODUCT_VERSION};                     ///< 产品版本。
#ifdef _MSC_VER
constexpr std::uint16_t product_version_major {PRODUCT_VERSION_MAJOR};  ///< 产品主版本号。
constexpr std::uint16_t product_version_minor {PRODUCT_VERSION_MINOR};  ///< 产品次版本号。
constexpr std::uint16_t product_version_patch {PRODUCT_VERSION_PATCH};  ///< 产品补丁版本号。
constexpr std::uint16_t product_version_tweak {PRODUCT_VERSION_TWEAK};  ///< 产品微调版本号。
#else
constexpr std::intmax_t product_version_major {PRODUCT_VERSION_MAJOR};  ///< 产品主版本号。
constexpr std::intmax_t product_version_minor {PRODUCT_VERSION_MINOR};  ///< 产品次版本号。
constexpr std::intmax_t product_version_patch {PRODUCT_VERSION_PATCH};  ///< 产品补丁版本号。
constexpr std::intmax_t product_version_tweak {PRODUCT_VERSION_TWEAK};  ///< 产品微调版本号。
#endif
constexpr char product_authors[] {PRODUCT_AUTHORS};                     ///< 产品作者。
constexpr char product_license[] {PRODUCT_LICENSE};                     ///< 产品许可。
}

/// \brief 项目名。
#define PROJECT_NAME          "@PROJECT_NAME@"

/// \brief 项目版本。
#define PROJECT_VERSION       "@PROJECT_VERSION@"

/// \brief 项目主版本号。
#define PROJECT_VERSION_MAJOR @PROJECT_VERSION_MAJOR@

/// \brief 项目次版本号。
#define PROJECT_VERSION_MINOR @PROJECT_VERSION_MINOR@

/// \brief 项目补丁版本号。
#define PROJECT_VERSION_PATCH @PROJECT_VERSION_PATCH@

/// \brief 项目微调版本号。
#define PROJECT_VERSION_TWEAK @PROJECT_VERSION_TWEAK@

/// \brief 项目作者。
#define PROJECT_AUTHORS       "@PROJECT_AUTHORS@"

/// \brief 项目许可。
#define PROJECT_LICENSE       "@PROJECT_LICENSE@"

namespace rrlibx {
constexpr char project_name[] {PROJECT_NAME};                           ///< 项目名。
constexpr char project_version[] {PROJECT_VERSION};                     ///< 项目版本。
#ifdef _MSC_VER
constexpr std::uint16_t project_version_major {PROJECT_VERSION_MAJOR};  ///< 项目主版本号。
constexpr std::uint16_t project_version_minor {PROJECT_VERSION_MINOR};  ///< 项目次版本号。
constexpr std::uint16_t project_version_patch {PROJECT_VERSION_PATCH};  ///< 项目补丁版本号。
constexpr std::uint16_t project_version_tweak {PROJECT_VERSION_TWEAK};  ///< 项目微调版本号。
#else
constexpr std::intmax_t project_version_major {PROJECT_VERSION_MAJOR};  ///< 项目主版本号。
constexpr std::intmax_t project_version_minor {PROJECT_VERSION_MINOR};  ///< 项目次版本号。
constexpr std::intmax_t project_version_patch {PROJECT_VERSION_PATCH};  ///< 项目补丁版本号。
constexpr std::intmax_t project_version_tweak {PROJECT_VERSION_TWEAK};  ///< 项目微调版本号。
#endif
constexpr char project_authors[] {PROJECT_AUTHORS};                     ///< 项目作者。
constexpr char project_license[] {PROJECT_LICENSE};                     ///< 项目许可。
}

#endif
